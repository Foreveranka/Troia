dist
node_modules
target
pnpm-lock.yaml
*.wasm
# Generated by `just bootstrap` (scripts/write-deployment.mjs). Prettier must not reformat a data record
# whose writer is a script — the two would fight, and `just bootstrap` would leave the gate red.
deployment.testnet.json

# Written by `scripts/wire-apps.mjs`. A generator and a formatter must not both own a file's shape: whichever
# ran last would leave the gate red. Drift is caught by packages/config/test/deployment-pinning.spec.ts instead.
app/storefront/src/deployment.generated.ts
app/extension/src/lib/deployment.generated.ts
